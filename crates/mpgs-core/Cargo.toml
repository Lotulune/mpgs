[package]
name = "mpgs-core"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
time = { version = "0.3.47", features = ["formatting", "macros", "parsing"] }
utoipa = "5"

[dev-dependencies]
serde_json = "1"
