{
  "$schema": "gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Minimal permissions for the MPGS main window: core defaults plus opening Steam store pages in the system browser. No shell, fs, or arbitrary HTTP capabilities.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "opener:allow-open-url",
      "allow": [
        { "url": "https://store.steampowered.com/*" },
        { "url": "steam://*" }
      ]
    }
  ]
}
